## Sprint / task

<!-- e.g. Sprint 1.2 — Core domain model -->

## Summary

<!-- What does this PR do? -->

## Checklist

- [ ] `uv run ruff check .` passes
- [ ] `uv run black --check .` passes
- [ ] `uv run mypy src tests` passes
- [ ] `uv run pytest` passes
- [ ] New/changed logic has tests
- [ ] Layering rules respected (see `docs/ARCHITECTURE.md`)
- [ ] Docs updated if behavior or architecture changed
